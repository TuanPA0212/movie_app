import 'package:flutter/material.dart';

class NewAndHotTab extends StatelessWidget {
  const NewAndHotTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(
      child: Text("new and hot"),
    );
  }
}
